@model IEnumerable<string>

<h2 class="list-group-item-heading">Kategorie</h2>
<ul class="list-group">
    @foreach (var cat in Model)
    {
        <li class="list-group-item">@Html.ActionLink(cat, "List", "Product", new { category = cat }, null)</li>
    }
</ul>